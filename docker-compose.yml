services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin  
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: market_cosmo
    ports:
      - "5466:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
